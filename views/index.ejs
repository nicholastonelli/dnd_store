<% items.forEach((item, index) => { %>
<h1>
  <a href="/items/<%= item.id %>"><%= item.name %></a>
</h1>
<form action="/items/<%= item.id %>?_method=DELETE" method="POST">
  <input type="submit" value="DELETE" />
</form>

<a href="/items/<%= item.id %>/edit">edit me</a>
<% }) %>
